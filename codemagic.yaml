#workflows:
#  build-and-upload-aab:
#    name: Build & Upload AAB
#    max_build_duration: 60
#    instance_type: mac_mini_m2
#
#    environment:
#      android_signing:
#        - keystore_reference  # Replace with your keystore ID
#      groups:
#        - test
##      vars:
##        SLACK_WEBHOOK_URL: $SLACK_WEBHOOK_URL  # This is where the environment variable will be accessed
#
#    scripts:
#      - name: Set Android SDK location
#        script: |
#          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/local.properties"
#
#      - name: Build signed release AAB
#        script: |
#          ./gradlew bundleRelease
#
#      - name: Send Slack Message (On Success)
#        script: |
#          echo "Sending Slack message..."
#          echo "SLACK_WEBHOOK_URL: $SLACK_WEBHOOK_URL"  # Debug line to check the URL
#          if [ -z "$SLACK_WEBHOOK_URL" ]; then
#            echo "Error: $SLACK_WEBHOOK_URL is not set!"
#            exit 1
#          fi
#
#          # Send Slack message via curl
#          curl -X POST -H 'Content-type: application/json' \
#          --data '{"text": "Build Success! :white_check_mark:"}' \
#          $SLACK_WEBHOOK_URL
#
#          if [ $? -ne 0 ]; then
#            echo "Error: Slack notification failed! $SLACK_WEBHOOK_URL"
#            exit 2
#          fi
#          echo "Slack message sent successfully!"
#
#    artifacts:
#      - app/build/outputs/**/*.aab
workflows:
  build-and-upload-aab:
    name: Build & Upload AAB
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      android_signing:
        - keystore_reference  # Replace with your keystore ID
      groups:
        - test

    scripts:
      - name: Set Android SDK location
        script: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > "$CM_BUILD_DIR/local.properties"

      - name: Build signed release AAB
        script: |
          ./gradlew bundleRelease

      - name: Send Slack Message (On Success)
        script: |
          echo "Sending Slack message..."

          # Fetch the version info from the build.gradle
          VERSION_CODE=$(grep versionCode app/build.gradle | awk '{print $2}' | tr -d ';')
          VERSION_NAME=$(grep versionName app/build.gradle | awk '{print $2}' | tr -d '"')
          
          # Fetch the .aab file path (replace with your actual path if different)
          AAB_FILE_PATH="app/build/outputs/bundle/release/app-release.aab"
          
          # Construct the download link (example, replace with your actual hosting link)
          DOWNLOAD_LINK="https://your-download-link.com/$AAB_FILE_PATH"

          # If SLACK_WEBHOOK_URL is not set, exit the script
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "Error: SLACK_WEBHOOK_URL is not set!"
            exit 1
          fi

          # Prepare the Slack message with the version and download link
          SLACK_MESSAGE="Build Success! :white_check_mark:
          Version: $VERSION_NAME (Code: $VERSION_CODE)
          Download the AAB here: $DOWNLOAD_LINK"

          # Send the message to Slack
          curl -X POST -H 'Content-type: application/json' \
          --data '{"text": "'"$SLACK_MESSAGE"'"}' \
          $SLACK_WEBHOOK_URL

          if [ $? -ne 0 ]; then
            echo "Error: Slack notification failed!"
            exit 2
          fi
          echo "Slack message sent successfully!"

    artifacts:
      - app/build/outputs/**/*.aab
